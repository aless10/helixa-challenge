[app]
env=production
debug=false
testing=false

[logging]
log_conf_path=%(LOG_CONF)s

[cache]
use_cache=%(USE_REDIS_CACHE)s
redis_host=%(REDIS_HOST_ADDRESS)s
redis_port=%(REDIS_PORT_BIND)s

[database]
db_connection = %(DATABASE_CONNECTION)s